# ADR-001: 결과지 narrative의 LLM 프레젠테이션 레이어 분리

**Status:** Proposed
**Date:** 2026-06-08
**Decider:** 세이지

## Context

결과지의 추천 로직(ResultSpine 엔진)은 결정론적이고 테스트 1,669개가 보장한다. 그러나 사용자에게 보이는 문장은 미리 써둔 조각의 템플릿 조립이라 (1) 한 사람이 쓴 글처럼 읽히지 않고, (2) "왜 이 추천인지"가 답변과 연결돼 보이지 않으며 (전문 자문/강의가 갑자기 등장하는 문제), (3) 조립 버그(이중 '지만')가 구조적으로 재발 가능하다. 사용자 피드백: "헤드라인은 와닿는데 세부가 조각 조합이라 와닿지 않는다."

제약: Vite 클라이언트 앱(API 키 노출 불가), 솔로 개발, 무료 서비스라 호출당 비용이 민감, 결과 일관성(같은 답변 = 같은 추천)은 절대 양보 불가.

## Decision

**결정은 엔진, 문장은 LLM.** 엔진의 ResultSpine을 그대로 유지하고, 그 위에 프레젠테이션 레이어로 Vercel 서버리스 함수 `/api/narrative`를 추가한다. LLM은 스파인의 사실을 재서술만 하고, 추천·게이트·플랜을 변경할 수 없다. 실패 시 현재 템플릿 문장이 폴백이다.

## Options Considered

### Option A: LLM이 결과 전체를 생성 (엔진 대체)

| 차원 | 평가 |
|---|---|
| 복잡도 | 낮아 보이나 실제로는 높음 (평가·회귀 체계 필요) |
| 비용 | 호출당 높음 (긴 출력) |
| 일관성 | **불가** — 같은 답변에 다른 추천 가능 |
| 테스트 | 기존 1,669개 무효화 |

**Pros:** 가장 자연스러운 글. **Cons:** 추천 신뢰성 붕괴, 게이트 불변식(번아웃→회복 우선 등) 보장 불가, 환각 추천 리스크. **기각.**

### Option B: 엔진 유지 + LLM은 narrative 재서술 (선택)

| 차원 | 평가 |
|---|---|
| 복잡도 | 중 (서버리스 함수 1개 + 검증 + 폴백) |
| 비용 | 결과지당 ₩5~15 (Haiku, 입력 ~3.5k/출력 ~700토큰) |
| 일관성 | 추천은 100% 결정론 유지 |
| 폴백 | 현 템플릿이 그대로 — 0 리스크 배포 가능 |

**Pros:** 조각 조합 문제 해결, 답변 근거를 문장에 자연스럽게 엮음(provenance), 기존 테스트 자산 유지, 유료 티어 경계 자연 형성. **Cons:** 지연 2~4초(스트리밍/스켈레톤 필요), 출력 검증 로직 필요.

### Option C: 템플릿 고도화 (LLM 없이 조각 수를 늘림)

**Pros:** 비용 0, 지연 0. **Cons:** 조합 폭발 — 직무 12종 × 유형 9종 × 게이트 조합이면 수천 문장을 손으로 써야 하고, 그래도 "조립 느낌"은 남는다. 이미 한계 확인됨. **기각.**

## 아키텍처

```
[클라이언트]                         [Vercel 서버리스]            [Anthropic API]
ResultSpine 생성 (기존 엔진)
    │
    ├─ 즉시 렌더: 템플릿 narrative (현재 코드 = 스켈레톤 겸 폴백)
    │
    └─ POST /api/narrative ────────▶ 페이로드 검증
         (spine 발췌 + 답변 라벨)      프롬프트 조립 ──────────▶ Haiku 호출
                                      출력 검증 (아래 규칙)
    부드럽게 교체 ◀───────────────── JSON 반환 (실패 시 4xx → 폴백 유지)
```

- 키는 서버리스 환경변수(`ANTHROPIC_API_KEY`)에만 존재.
- 캐시: 페이로드 해시 → Vercel KV(또는 응답 헤더 캐시). 같은 답변 조합 재호출 방지.
- 클라이언트는 LLM 응답이 오기 전까지 현재 템플릿을 보여주므로 체감 지연이 없다.

## 입력 페이로드 (엔진 → LLM에 주는 것)

```jsonc
{
  "profile": {
    "jobRoleRaw": "서비스 기획자", "ageBand": "30_late", "workMode": "조직 소속",
    "totalCareerStage": "7~12년", "currentFieldStage": "1~3년",   // 둘의 차이가 이력 구조 추론의 핵심 재료
    "jobRoleTraits": "전문직 아님 / 자격 불필요"                    // jobRoleNormalizer 카테고리에서 도출
  },
  "recommendation": {                        // 스파인 발췌 — LLM이 바꿀 수 없는 사실
    "currentBestMove": "이직",
    "strategicDirection": "콘텐츠/퍼스널 브랜드 (조건부)",
    "coreExperiment": "짧은 콘텐츠 몇 개를 올려 가볍게 반응 보기",
    "conditionalOption": { "label": "전문 자문/강의", "blockedBy": "시장 검증 필요" },
    "confidenceBand": "중간",
    "mainType": "정체된 성실형(내부) / 도약 준비 성실형(표시)"
  },
  "answerHighlights": [                      // provenance — 추천에 영향을 준 답변의 한국어 라벨
    "끌리는 역할: 전문가, 창업가",
    "가치 1순위: 수익, 2순위: 의미",
    "시장 검증: 아직 초기",
    "에너지: 높음", "감당 가능한 리스크: 수입 일부"
  ],
  "constructSignals": ["실행 자신감 높음", "방향 통제감 낮음"],
  "userConcern": "회사를 계속 다녀야 할지 내 걸 해야 할지 모르겠어요"  // 자유 텍스트 (신규 수집, 선택 입력)
}
```

## 프롬프트 초안

**system:**
> 너는 커리어 상담사다. 입력으로 받은 recommendation의 사실 범위 안에서, 사용자의 답변 조합으로부터 본인이 언어화하지 못한 패턴을 찾아내 결과지를 쓴다.
>
> 핵심 임무 — **요약 금지, 해석 의무**. 출력 전에 아래 3단계 추론을 순서대로 수행하라:
>
> **1단계 — 이력 구조 추론**: profile 필드를 교차 대조하라. 직군의 특성(전문직/자격 필요 여부), 현 분야 경력 vs 총 경력의 차이, 연령대를 함께 보면 살아온 구조가 보인다. 예: 수의사 + 현 분야 2년 + 총 10년 → 자격까지 갖춰 방향을 바꾼 경험 → 도전을 두려워하지 않거나, 깊은 고민을 거쳐온 사람.
>
> **2단계 — 동기 구조 추론**: 가치 우선순위 + 자신감 신호 + 끌리는 역할의 조합에서 욕구 가설을 세워라. 예: 영향력·수익 동시 상위 + 자신감 높음 → 잘하는 게 많고 자존감이 높은 사람의 고민은 '무엇을 할까'가 아니라 '어떻게 더 크게 인정받고 벌까'다.
>
> **3단계 — 진짜 질문 재정의**: 1·2단계를 합쳐, 사용자가 명시하지 않은 '진짜 질문'을 한 문장으로 정의하고 이것을 coreInsight로 출력하라. 사용자의 고민 문장(userConcern)을 받아 적지 말고 그 아래의 질문으로 내려가라.
>
> 제약: 답변을 나열하거나 되풀이하지 마라 ("~라고 답하셨죠"의 연속은 실패다). 인용은 근거 제시용으로 최대 2회. 모든 추론은 입력 신호 **2개 이상의 조합**에서 도출돼야 하고, 이력 추론처럼 빗나갈 수 있는 해석은 "~였을 가능성이 높아요. 만약 그렇다면" 화법으로 가설임을 드러낸다.
> - coreInsight의 렌즈로 recommendation을 **재해석**하라 — 추천을 바꾸는 게 아니라, 같은 실험을 사용자의 결에 맞게 읽는 법을 제시한다 (예: "나를 알리는 글 4개"가 아니라 "만든 것을 보여주는 글 4개").
> - **narrative 구조 강제**: 인사이트를 재설명하지 마라 (coreInsight 박스가 이미 말했다). 문단 1 = "그래서 이렇게 하시면 돼요" (재해석된 행동 지시, 구체적으로), 문단 2(선택) = 안전판·여건 한 줄. 한 문단에 한 가지 생각만.
> - userConcern이 있으면 결과지 어딘가에서 **사용자의 표현을 그대로 한 번 되비춰** 그 고민에 직접 답하라 ("'회사를 계속 다녀야 할지…'라고 적으셨죠. 이 결과지의 답은 ~예요"). 단, 고민 문장을 분석 대상으로 삼되 내용을 왜곡하지 마라.
>
> 절대 규칙:
> 1. 추천을 바꾸거나 새 선택지를 만들지 않는다. recommendation에 없는 행동을 제안하지 않는다.
> 2. 사용자가 답하지 않은 사실을 지어내지 않는다. 해석은 허용, 사실 날조는 금지.
> 3. 추론된 후보(conditionalOption 등)는 추론임을 밝힌다 ("점수상 떠오른 후보예요. 끌리지 않으면 지우셔도 됩니다").
> 4. 톤(Tone A): 친구의 조언처럼 따뜻하고 직접적으로. 허락을 주는 화법. 데이터 용어·점수·내부 유형명 금지.
> 5. 의료·심리 진단 표현 금지.
> 6. 출력은 JSON만. coreInsight 1문장, narrative 4~7문장, whyBullets 각 1문장 × 3~4개.
>
> 출력 형식: {"coreInsight": string, "narrative": string, "whyBullets": string[]}

**few-shot (system에 포함, 2개)** — 추론 품질을 결정하는 가장 강한 레버. 창업자가 직접 쓴 예시를 그대로 사용:

> 예시 1 (이력 구조): 수의사 + 현 분야 2년 + 총 경력 10년 → "자격까지 갖춰 방향을 바꾼 경험이 있는 분 → 도전 자체를 두려워하지 않는 사람일 가능성. 진짜 질문: 새 길 찾기가 아니라, 쌓아온 것을 어떻게 더 크게 만들까."
>
> 예시 2 (동기 구조): 영향력·수익 동시 상위 + 자신감 높음 → "잘하는 게 많고 자존감이 높은 분 → 명예욕 또는 수익 극대화가 핵심 동기. 진짜 질문: 무엇을 할까가 아니라, 어떻게 더 크게 인정받고 벌까."

**user:** (위 페이로드 JSON)

**기대 출력 예시:**
```json
{
  "narrative": "서비스 기획자로 7년 넘게 일해온 분이고, 전문가로 인정받는 역할과 직접 만드는 일에 끌린다고 하셨죠. 그 방향 자체는 맞아요. 다만 아직 시장 반응을 확인해 본 적이 없다고 하셨으니, 이번 달은 수입을 지키는 이직 준비를 바닥에 깔고 콘텐츠 몇 개로 가볍게 반응을 확인하는 게 순서예요. 근거가 절반쯤 모였고, 나머지는 이 30일이 채워 줍니다.",
  "whyBullets": [
    "전문가·창업가 역할에 끌린다고 답하셔서 자문/강의도 후보에 올랐어요 — 다만 시장 반응 확인 전이라 조건부로 두었어요.",
    "..."
  ]
}
```

## 출력 검증 규칙 (서버리스에서, 통과 못 하면 폴백)

1. JSON 파싱 성공 + 필드 타입 일치
2. `currentBestMove`·`coreExperiment` 라벨이 narrative 또는 whyBullets에 등장해야 함 (추천 누락 방지)
3. 금지어 검사: recommendation에 없는 옵션 라벨(전체 옵션 사전과 대조), "반드시", "100%", 진단성 표현
4. 길이 제한: coreInsight ≤ 80자, narrative ≤ 500자, bullet ≤ 90자
5. (P1) 라이트 검증 모델 호출 또는 휴리스틱으로 사실 위반 샘플링 점검

## 인사이트 모드의 추가 리스크 (요약 모드보다 한 단계 위험)

해석을 요구하는 만큼 **과잉 해석** 리스크가 생긴다. 완화책:

- 인사이트는 답변 2개 이상의 조합에서만 도출 (프롬프트 강제) + 골든 페이로드 리뷰에서 "이 인사이트의 근거 답변이 뭐냐"를 역추적할 수 없으면 불합격
- 헤지 화법 강제 ("~일 가능성이 높아요") — 단정형 출력은 검증에서 반려
- 모델: 해석 품질이 핵심이므로 **이 레이어는 Haiku 대신 Sonnet 권장** (결과지당 ₩30~60 수준으로 상승하나, 유료 티어 재료이기도 함). 무료 트래픽엔 Haiku + 단순 프롬프트, 유료/심층에 Sonnet 이원화도 가능

## Consequences

- **쉬워지는 것**: 직무·답변 맞춤 문장(시안 B·C가 코드 없이 해결), provenance 설명, 카피 품질 개선이 프롬프트 수정으로 가능
- **어려워지는 것**: 응답 품질의 비결정성 → 골든 페이로드 10개에 대한 출력 스냅샷 리뷰를 배포 전 절차로 추가, 운영 비용 발생(무료 트래픽 급증 시 호출 상한 필요)
- **다시 볼 것**: 호출량이 커지면 캐시 적중률·모델 다운그레이드 검토. 유료 티어에서는 Sonnet급 + 사주 데이터 결합으로 차별화

## 유료화 경계 (이 설계가 만드는 것)

- 무료: 템플릿 narrative (현재)
- 무료+: Haiku 재서술 (전환 미끼 — 품질 차이를 맛보게)
- 유료: 심층 해석 (Sonnet급, 사주·만세력 결합, 30일 팔로업 리포트) — `buildDeepPersonaNarrative`·`careerCompassFollowUp30d`가 입력 재료로 합류

## 신규 입력: 고민 한 문장 (자유 텍스트)

- 챗/하이브리드 오프닝에 선택 입력으로 추가: "지금 어떤 고민으로 오셨어요? 한 문장이면 충분해요." (건너뛰기 가능)
- `UserProfile.concernFreeText`로 저장 — 기존 계약대로 **엔진은 절대 소비하지 않음** (라우팅 핑거프린트 불변), LLM 페이로드에만 전달
- 효과: 사용자의 실제 언어가 들어와 인사이트·되비추기 품질이 올라가고, 결과지가 "내 질문에 답해줬다"가 됨
- 프라이버시: 자유 텍스트가 외부 API로 전송되므로 입력란 아래 고지 1줄 + 개인정보(이름·회사명) 입력 자제 안내

## Action Items

1. [ ] `api/narrative.ts` 서버리스 함수 + 입력/출력 검증 (P0)
2. [ ] ResultSpineView에 coreInsight 박스 + 교체 렌더(템플릿 → LLM 응답 fade-in) + 실패 폴백 (P0)
3. [ ] 챗/하이브리드 오프닝에 '고민 한 문장' 선택 입력 + `concernFreeText` 패스스루 (P0)
4. [ ] 골든 페이로드 10개 작성 (기존 테스트 픽스처 + userConcern 변형) + 출력 리뷰 (P0)
5. [ ] 페이로드 해시 캐시 (P1)
6. [ ] whyBullets 교체 + provenance 칩 (P1)
7. [ ] 유료 티어 심층 해석 프롬프트 (P2)
