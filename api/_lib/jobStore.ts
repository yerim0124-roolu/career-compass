// api/_lib/jobStore.ts — DEPRECATED / UNUSED.
//
// job 저장소는 각 엔드포인트에 인라인되었다(self-contained 원칙: 상대경로 src/api import 금지,
// ERR_MODULE_NOT_FOUND 회피). _lib/ 는 Vercel 라우트가 아니며 아무도 import하지 않는다.
// 샌드박스에서 파일 삭제가 막혀 빈 모듈로 남겨둔다.
export {};
