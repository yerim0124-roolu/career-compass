// ─────────────────────────────────────────────────────────────────────────────
// closingMessages.ts — 마무리 인용 카드(ClosingQuoteCard) 문구.
//
// 줄바꿈을 \n 으로 보존한다(컴포넌트가 white-space: pre-line 으로 렌더).
// 엔진의 executionPlan.closingLine(4개 변형: 정리/실험/후보생성/회복)을 키로 매핑하고,
// 매칭이 없으면 원문(단일 줄, 자동 줄바꿈)으로 폴백한다.
// ─────────────────────────────────────────────────────────────────────────────

export const CLOSING_MESSAGES: Record<string, string> = {
  // essentialist — 정리형
  '다 붙잡으려 애쓰지 않아도 괜찮아요. 이번 달은 마음이 가는 두세 개만 곁에 남겨도, 충분히 잘 가고 있는 거예요.':
    '다 붙잡으려 애쓰지 않아도 괜찮아요.\n이번 달은 마음이 가는 두세 개만\n곁에 남겨도,\n충분히 잘 가고 있는 거예요.',
  // actionType — 작은 실험형
  '완벽하지 않아도 괜찮아요. 이번 달은 작게 한 번 부딪혀 신호 한 줄만 남겨도, 그걸로 충분합니다.':
    '완벽하지 않아도 괜찮아요.\n이번 달은 작게 한 번 부딪혀\n신호 한 줄만 남겨도,\n그걸로 충분합니다.',
  // optionGeneration — 후보 생성형
  '지금 답을 내지 않아도 괜찮아요. 해보고 싶은 길이 두 개만 생겨도, 이번 달은 충분히 의미 있어요.':
    '지금 답을 내지 않아도 괜찮아요.\n해보고 싶은 길이 두 개만 생겨도,\n이번 달은 충분히 의미 있어요.',
  // recovery — 회복형
  '지금은 잠시 멈춰도 괜찮아요. 새 일을 벌이기보다 흐트러진 리듬을 되찾는 것만으로, 이번 달은 충분합니다.':
    '지금은 잠시 멈춰도 괜찮아요.\n새 일을 벌이기보다\n흐트러진 리듬을 되찾는 것만으로,\n이번 달은 충분합니다.',
};

/** closingLine(엔진 변형 문장)을 줄바꿈 보존 문구로. 미등록이면 원문 그대로 반환(자동 줄바꿈). */
export function getClosingMessage(closingLine: string): string {
  return CLOSING_MESSAGES[closingLine] ?? closingLine;
}
